<%= form_for @product , url: {action: :create} do |f| %>
    <p><%= f.label :name, '商品名' %>
    <%= f.text_field :name %></p>
    <p>
    <%= f.label :price, '値段' %>
    <%= f.password_field :price %>
    </p>
  
     <%= f.submit '作成' %>
     
<% end %>